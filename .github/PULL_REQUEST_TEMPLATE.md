# Pull Request

<!--
Thanks for the PR. A few things to check before submitting:

  - [ ] `make check` passes
  - [ ] `make lint` passes (or `make install-hooks` so it runs on
        every commit automatically)
  - [ ] `make test` passes (152+ tests, all green)
  - [ ] If you changed anything in the build/run chain,
        `make test-docker` passes on a host with Docker
  - [ ] CHANGELOG entry added under `[Unreleased]` (Keep a Changelog)
  - [ ] If you touched docker-compose.yml or base.Dockerfile in a
        security-relevant way, `docs/DESIGN.md` is updated to match

See CONTRIBUTING.md for full guidelines.
-->

## Summary

<!-- What this PR changes and why. The "why" matters more than the "what". -->

## Test plan

<!-- How you verified the change works. For non-trivial changes,
     prefer tests added under tests/ over manual steps. -->

## Notes

<!-- Anything reviewers should know: design tradeoffs considered,
     followups deferred, related issues, etc. -->
