---
name: Feature request
about: A capability connie doesn't have yet
title: ''
labels: enhancement
---

## What problem does this solve

<!-- The user-facing situation that the feature would address.
     Concrete is better than abstract. -->

## Proposed shape

<!-- What would the CLI / config / behaviour look like?
     Sketch is fine; the discussion can refine the details. -->

```sh
# Example invocation, if applicable
```

## Alternatives considered

<!-- Other ways the problem could be solved — workarounds you've tried,
     existing tools that do something similar, or designs you ruled out
     and why. -->

## Scope check

<!-- Optional: how does this fit (or not fit) with connie's design
     principles in docs/DESIGN.md? In particular:
       - Does it require capabilities currently dropped by `cap_drop: ALL`?
       - Does it write outside `/workspace`, `~/.claude/`, `/tmp`?
       - Does it require state in the project directory? (Non-invasive)
-->
